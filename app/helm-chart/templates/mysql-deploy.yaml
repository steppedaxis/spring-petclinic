apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.name }}-mysql
spec:                               
  replicas: 1
  selector:
    matchLabels:
      app: {{ .Values.db.mysqlLabelSelector }}
  template:
    metadata:
      labels:
        app: {{ .Values.db.mysqlLabel }}
    spec:
      containers:
      - name: mysql
        image: {{ .Values.db.mysqlImage }}
        env:
        - name: MYSQL_ROOT_PASSWORD
          value: {{ .Values.db.MYSQL_ROOT_PASSWORD }} 
        - name: MYSQL_ALLOW_EMPTY_PASSWORD
          value: {{ .Values.db.MYSQL_ALLOW_EMPTY_PASSWORD }}
        - name: MYSQL_USER
          value: {{ .Values.db.MYSQL_USER }}
        - name: MYSQL_PASSWORD
          value: {{ .Values.db.MYSQL_PASSWORD }}
        - name: MYSQL_DATABASE
          value: {{ .Values.db.MYSQL_DATABASE }}
        ports:
        - containerPort: 3306
